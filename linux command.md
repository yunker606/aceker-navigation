# linux(kali/ubuntu) command
## 单个命令
pwd {查看所处路径} <br>
上箭头/下箭头 {上一个命令/下一个命令} <br>
tab {补齐预设} <br>
touch {创建文件} <br>
mkdir {创建文件夹} <br>
## cd {转路径}
cd /aa {转到对应路径} <br>
~ {回控制台} <br>
.. {回上一个访问路径} <br>
## ls (列出当前路径下所有文件夹)
-l {long,长格式展示：权限、所有者、组、大小、时间、文件名｝ <br>
-a {all，显示所有文件，包括隐藏文件（.开头的文件）} <br>
-lh/-ah {human,人性化显示（h一般不单独用）} <br>
-t {time,按时间修改顺序，最新放最上面} <br>
-tr {reverse，按时间反向排序（旧的在前）} <br>
-s {按文件大小排序，大文件在前} <br>
-r {reverse，反向排序（旧的在前）} <br>
-R {recursive 递归列出所有子目录（层层展开）} <br>
-d {只展示目录本身，不列出目录里面大文件} <br>
-i {显示id编号} <br>
-g {长格式，但不显示所有者} <br>
-o {长格式，但不显示所属组} <br>
-n {长格式，UID/GID（数字）} <br>
-F {文件末尾加标记：/目录 *可执行 @符号链接} <br>
-A {显示所有文件，包含隐藏文件} <br>
-1 {每行只显示一个文件（数字1，不是字母l）} <br>
## find /home  (按路径查找文件)
-iname  "文件名"不区分大小写查找文件名 <br>
-name  "带通配符的文件名"模糊搜索 <br>
-name  "文件名"精确查找 <br>
-size +1（大于512字节的文件） <br>
-size -1（小于512字节的文件） <br>
1 一个数据块等于512byte <br>
e.g.=6 (等于6个数据块，等于3k) <br>
    +6（大于6个数据块，大于3k） <br>
    -6（小于6个数据块，小于3k） <br>
-user aa（查找home下aa用户的文件） <br>
-mmin -5（五分钟内）加号是大于5分钟 <br>
-type f（file文件） <br>
-type d（directory文件夹） <br>
-type l（link软链接） <br>
## 更改文件属性
chgrp -R （ 组 ) /aa/cc （-R是归递文件夹内所有文件，可以不加，同时只更改文件时不用加）
chown -R user
## rm ()
